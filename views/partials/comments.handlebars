<div class="comments nav-bar reviews">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.username}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>